import test from "node:test";
import assert from "node:assert/strict";
import { search } from "./search.js";

test.only("searchs pokemons", () => {
    selectedType, 
    displayedPokemons, 
    allPokemons, 
    loadedCount
    assert.deepStrictEqual(search());
});